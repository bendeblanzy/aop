{
  "title": "Input — Atexo MPE Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "providers": {
      "title": "Plateformes à scraper",
      "type": "array",
      "description": "Liste des plateformes Atexo MPE à scraper. Chaque entrée : { id, baseUrl }.",
      "editor": "json",
      "default": [
        { "id": "place", "baseUrl": "https://www.marches-publics.gouv.fr" },
        { "id": "mxm", "baseUrl": "https://marches.maximilien.fr" }
      ]
    },
    "filters": {
      "title": "Filtres",
      "type": "object",
      "description": "Filtres appliqués au scraping (catégorie de marché, fraîcheur).",
      "editor": "json",
      "default": { "categorie": "services", "maxAgeDays": 7 }
    },
    "maxPagesPerProvider": {
      "title": "Pages max par plateforme",
      "type": "integer",
      "description": "Garde-fou : nombre maximum de pages de listing à parcourir par plateforme.",
      "editor": "number",
      "default": 50,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": ["providers"]
}
